apiVersion: v2
name: cicd-demo
description: Production-grade CI/CD demo service (FastAPI) — Helm chart

# There are TWO versions and they mean different things:
#   version    -> the version of THIS CHART (bumped when templates change)
#   appVersion -> the version of the APP the chart deploys (the default image tag)
type: application
version: 0.1.0
appVersion: "1.0.3"
