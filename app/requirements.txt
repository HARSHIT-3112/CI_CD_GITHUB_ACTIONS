# Runtime dependencies ONLY. These are what ends up in the production image.
# Pinned to exact versions for reproducible, deterministic builds — a core
# requirement of a trustworthy pipeline (the image built today == the image
# built next month from the same commit).
fastapi==0.139.2
uvicorn[standard]==0.51.0
